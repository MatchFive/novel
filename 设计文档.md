# 长篇小说 AI 创作工作台 — 设计文档

> 版本：v1.5　|　状态：评审稿　|　技术路线：Python 3.11 + PySide6 桌面应用 + 云端 LLM（OpenAI 兼容协议）+ SQLite 本地存储（单机单用户）
>
> v1.1 变更：新增**三层记忆体系**（剧情事件日志 / 角色经历档案 / 知识状态与承诺跟踪），解决正文写作时 LLM"失忆"导致的前后文逻辑断裂问题。
>
> v1.2 变更：明确用户与 LLM 的**两种交互执行路径**（单次直连 / 多步工作流图），引入 **LangGraph 工作流编排层**，将人机确认点（interrupt）与断点续跑（checkpointer）框架化。
>
> v1.3 变更：①新增**AI 指令驱动编辑**（自然语言直接修改设定/剧情，LLM 自主定位变更范围，用户确认计划后执行）；②新增**生成配置**（行文风格/每章字数/视角等参数化）；③新增**设定沉淀**（生成内容中涌现的新设定可一键入库）；④**基础提示词（base_prompt）界面化可编辑** + **写作教程知识库导入**。
>
> v1.4 变更：明确**数据组织层级**——一本长篇 = 一个项目（全局层与项目层分离）；多本书管理；**系列作品复用暂不处理**（后续如有需求再评估）。
>
> v1.5 变更：新增 **4.4 数据库访问规范（Repository 模式，强制）**——所有数据库操作收口于 `app/db/`（仓储层），禁止在 UI / Service / workflows / tools / 脚本层直接执行 SQL 或 ORM 查询；引入 UnitOfWork 事务边界与会话统一管理。
>
> v1.6 变更：新增 **6.1.1 模型路由（Model Routing）**——按任务难度/重要程度使用不同档位（lite/standard/pro）与不同厂商的模型；覆盖优先级、失败降级、成本分档统计。

---

## 1. 项目概述

### 1.1 产品定位

一款**面向长篇小说作者的个人 AI 创作工作台**。用户从"一个点子"出发，借助 LLM 在**世界观构筑 → 角色设计 → 大纲构造 → 章节划分 → 章节细纲 → 正文实现 → 修订与一致性检查**的全流程中获得逐步引导与内容生成，最终完成一部结构完整、前后一致的长篇小说。

产品本质是 **"人机协同的创作流水线"**：LLM 负责生成、发散、整理、细化与校验，用户负责把关方向、取舍与最终定稿（Human-in-the-loop）。每一步 AI 产出都先进入"建议草稿"，经用户确认后才写入正式数据，保证用户始终掌握创作主导权。

### 1.2 设计目标

| 目标 | 说明 |
|---|---|
| 全流程覆盖 | 从点子到正文的每个环节都有 LLM 参与，且产物可逐步向下游传递 |
| 长文本一致性 | 解决长篇小说"越写越崩"的问题：设定库 + **三层记忆体系** + 检索注入 + 滚动摘要 + 一致性检查 |
| **角色记忆连续** | **正文写作时 LLM 必须"记得"角色在前文经历过什么、说过什么、知道什么**，杜绝后文忘记/否认前文 |
| **自然语言驱动** | 用户可用一句话直接修改设定/剧情（AI 指令），无需手动定位条目；生成内容中的新设定可一键沉淀入库 |
| 用户可控 | 所有 AI 输出经"生成 → 预览 → 编辑 → 接受"四步确认 |
| 单机可用 | 数据完全本地存储，无需服务器，支持备份/恢复 |
| 模型无关 | 通过统一抽象层兼容 DeepSeek / 通义千问 / OpenAI / 智谱 / Kimi 等任意 OpenAI 兼容 API |

### 1.3 名词解释

| 术语 | 含义 |
|---|---|
| 设定库（Canon） | 世界观条目、角色卡等"静态作品事实"的总称，是检索与一致性校验的依据 |
| 剧情事件日志（Story Events） | **全知视角**的结构化事件记录：每章"发生了什么"，含参与角色、地点、新信息、伏笔 |
| 角色经历档案（Character Journey） | **限知视角**的记忆库：每个角色"经历过什么、说过什么话、做过什么事、此刻知道什么"，按章追加 |
| 知识状态（Knowledge State） | 信息项（秘密/真相/计划）与"哪个角色知道/不知道"的对应关系，用于防止"未卜先知"与"突然失忆" |
| 承诺/伏笔跟踪（Promises） | 角色立下的 flag、埋下的伏笔与未回收的悬念，正文生成时提醒推进或回收 |
| 视点角色（POV） | 当前章节叙事视角所跟随的角色，其经历档案获得最高注入优先级 |
| 细纲（Beat） | 章节内按场景拆分的叙事节拍（谁、在哪、发生什么、达成什么） |
| 卷 / 部（Arc） | 大于章节的叙事单元（如第一卷、第二部），承载独立的故事弧线 |
| 滚动摘要（Rolling Summary） | 对已写正文逐段压缩得到的分层摘要，用于压缩上下文（有损，仅作概览，**不替代经历档案**） |
| 建议草稿（Suggestion） | AI 生成但尚未写入正式数据的候选内容，可预览、编辑、接受或丢弃 |
| AI 指令（Command） | 用户用自然语言直接下达的修改指令（如"把反派的身份改成主角失散多年的哥哥"），由指令 Agent 自主定位影响范围、产出修改计划，用户确认后执行 |
| 设定沉淀（Canon Capture） | 生成内容（正文/大纲/细纲）中涌现的、设定库尚不存在的新设定（地点/物品/规则/组织等），检测去重后经用户勾选写入设定库，防止后续遗忘或再次发明 |
| 教程知识库（Handbook） | 导入的写作教程/指南文本（如网文创作指南），分类切片后按任务类型检索注入 prompt，增强大纲/正文/角色等各类生成的写作能力 |
| 生成配置（Generation Profile） | 生成时的参数化约束：行文风格、每章字数范围、视角、节奏、对话密度、温度等，作品级默认 + 单次生成可覆盖 |

### 1.4 核心问题与解决思路（v1.1）

**问题**：长篇小说正文动辄数十万字，无法整体进上下文；滚动摘要有损压缩会丢失细节。若正文写作只注入"世界观条目 + 角色卡 + 摘要"，LLM 并不知道某个角色**前文具体经历了什么、说过什么、知道什么**——于是角色后文否认前文言行、说出不该知道的信息、忘记自己的承诺，前后逻辑断裂成为必然。

**解决思路：三层记忆体系**，对应小说写作的"作者全知、角色限知"原则：

```
第 1 层  剧情事件日志（全知）  作品层面"发生了什么"，供作者与 LLM 全知使用
第 2 层  角色经历档案（限知）  每个角色"我经历了什么、我说过什么、我知道什么"，按视角写作时注入
第 3 层  知识状态 + 承诺跟踪   秘密/真相归谁知晓，flag 谁立下、何时回收
```

每章定稿后自动提取记忆（LLM 结构化抽取 + 用户可修正），正文生成时按"视点角色优先 + 检索命中"注入对应记忆，事后由一致性扫描兜底校验。

---

## 2. 用户与场景

### 2.1 目标用户

- 网文作者、长篇小说写作者（核心人群）
- 有创作点子但缺乏体系化展开能力的业余写手
- 需要批量产出大纲、细纲、初稿的"码字工作流"用户

### 2.2 核心使用场景

1. **从零立项**：只有一句话灵感 → AI 帮助扩展成可评估的点子，再逐步长成世界观与大纲。
2. **卡文续写**：写到一半写不下去 → 基于细纲与前情记忆，AI 提供续写、扩写、多分支选项。
3. **一致性维护**：写到第 40 章发现第 3 章设定对不上 → 一键全局一致性扫描（含记忆类冲突）。
4. **效率批量**：对已定大纲批量生成章节细纲、批量生成初稿（草稿质量模式）。
5. **润色修订**：对已写正文进行风格润色、矛盾修复、伏笔回收检查。

### 2.3 用户工作流总览

```
┌─────────┐   ┌──────────┐   ┌──────────┐   ┌────────────┐
│ 点子收集 │──▶│ 世界观构筑 │──▶│ 角色设计 │──▶│ 主线大纲构造 │
└─────────┘   └──────────┘   └──────────┘   └────────────┘
                                                    │
┌─────────┐   ┌──────────┐   ┌──────────┐   ┌───────▼────┐
│ 修订定稿 │◀──│ 一致性检查 │◀──│ 正文写作 │◀──│ 章节划分+细纲 │
└─────────┘   └──────────┘   └──────────┘   └────────────┘
                    ▲
        ┌───────────┴───────────┐
        │ 记忆提取（每章定稿后）  │──▶ 剧情事件日志 / 角色经历档案 /
        │ 事件→角色经历→知识→承诺 │    知识状态 / 承诺跟踪
        └───────────────────────┘
```

各阶段 LLM 参与方式（**所有步骤均有 LLM 参与**）：

| 阶段 | 用户输入 | LLM 参与内容 | 产出 |
|---|---|---|---|
| 点子 | 一句话 / 片段 / 关键词 | 扩展设定、头脑风暴衍生点子、评估潜力并打分排序 | 点子卡 |
| 世界观 | 点子 + 类型需求 | 生成设定条目、自动分类打标签、势力/地理/历史/规则体系化 | 结构化设定库 |
| 角色 | 角色需求（可选） | 生成角色卡（性格/背景/动机/弧光/能力）、自动生成角色关系网 | 角色卡 + 关系图 |
| 大纲 | 设定库 + 角色 | 提炼主题、设计主线冲突结构、生成卷级大纲 | 卷大纲 |
| 章节划分 | 卷大纲 | 将卷拆分为章节、给出每章目标与预计字数 | 章节计划 |
| 细纲 | 章节计划 + 设定 | 逐章生成场景级节拍（Beat），支持手动调整后重新生成 | 章节细纲 |
| 正文 | 细纲 + 检索到的设定与**角色记忆** | 逐章生成初稿、续写、扩写、多分支提供；严格遵循角色限知记忆 | 正文草稿 |
| 记忆提取 | 定稿章节 | 抽取事件日志、生成角色经历档案、更新知识状态与承诺状态 | 结构化记忆（自动+可修正） |
| 修订 | 全文 + 设定库 + 记忆库 | 一致性扫描（设定/时间线/**记忆冲突/知识泄漏/承诺回收**）、风格润色、矛盾修复建议 | 修订建议 / 定稿 |
| **指令驱动修改** | 任意自然语言指令 | 自主检索定位影响范围（设定/角色/章节/记忆），产出**修改计划**，确认后执行并出变更摘要 | 按指令修改后的数据 |
| **设定沉淀** | 新生成的正文/大纲/细纲 | 检测其中涌现的新设定，与设定库去重，生成待入库候选 | 用户勾选后写入设定库 |

---

## 3. 功能需求

### 3.1 功能总览

```
项目层：新建/打开/归档项目，项目仪表盘，设置
 ├─ 点子管理：点子卡 CRUD、AI 扩展、AI 评估、合并
 ├─ 世界观：分类条目 CRUD、AI 生成/扩写/拆条、一致性校验
 │    └─ 设定沉淀：生成内容中的新设定一键检测入库
 ├─ 角色：角色卡 CRUD、AI 生成、关系图、角色间对话模拟
 │    └─ 角色记忆：经历时间线、知识清单、承诺清单（查看/编辑/跳转正文）
 ├─ 大纲：卷级大纲、AI 生成、冲突结构编辑
 ├─ 章节：章节划分、细纲节拍、正文编辑、AI 续写/润色
 ├─ 记忆库：剧情事件日志（全知视图）、记忆提取任务、记忆修正
 ├─ 修订：全局一致性扫描（含记忆类）、修订建议、草稿对比
 ├─ AI 指令：全局指令栏（自然语言修改设定/剧情/正文），计划确认式执行
 ├─ 生成配置：行文风格/每章字数/视角/节奏等参数（作品级默认+单次覆盖）
 ├─ 教程知识库：写作教程导入（粘贴/文件/URL）、分类切片、按任务注入
 └─ 提示词管理：base_prompt 及各任务模板的界面化编辑、版本历史、恢复默认
系统层：模型配置、Token 用量、日志、备份/恢复
```

### 3.2 点子管理（Idea Board）

- 用户可快速记录零散灵感（一句话、图片描述、对话片段）。
- **AI 点子扩展**：输入一句子，LLM 返回"背景补全、冲突点、卖点、潜在世界观/角色方向"的结构化扩展。
- **AI 点子评估**：LLM 按"新颖度 / 可扩展性 / 市场吸引力 / 长篇潜力"四维打分并给出改进建议。
- **点子合并与转换**：可将 2~3 个点子合并为项目，或将点子直接"播种"成世界观/角色生成任务的输入。

### 3.3 世界观构筑（World Builder）

- 设定按**分类**组织（地理、历史、势力、种族、规则/魔法体系、科技、社会文化、事件等），分类可自定义。
- 条目包含：标题、内容、标签、重要性等级（1~5）、是否"铁律"（canonical，不可违背）。
- **AI 能力**：
  - 由点子一键生成"世界观骨架"（自动分类、批量建条目）；
  - 对单条目扩写、细化、补全；
  - 检测条目间矛盾（如时间线冲突、规则不自洽）；
  - 基于"铁律"条目校验新生成内容是否违反设定。
- **设定沉淀入口**：接受任何 AI 生成内容（正文/大纲/细纲）后，自动检测其中涌现的新设定并提示入库（见 3.12）。
- 所有条目进入设定库索引，供下游检索注入。

### 3.4 角色设计（Character Studio）

- 角色卡字段：姓名/别名、定位（主角/反派/配角/工具人）、外貌、性格维度（大五人格或 MBTI 可选）、背景故事、动机与目标、能力/金手指、口头禅/说话风格、弧光（成长线）、死亡/退场条件。
- **AI 能力**：
  - 生成完整角色卡（由设定库与大纲反向推导角色需求）；
  - 生成角色关系网（谁恨谁、谁欠谁、盟友/宿敌/师徒/恋人）并自动绘制关系图；
  - **角色对话模拟**：给定场景与两句台词，LLM 按角色性格补全对话，用于"对味"校验；
  - 角色成长弧光设计（起点 → 低谷 → 转变 → 终态）。
- **角色记忆**（见 3.4.1）：每个角色拥有自己的经历档案、知识清单与承诺清单，随剧情推进自动累积。

#### 3.4.1 角色记忆（Character Memory）

| 子模块 | 内容 | 用户可操作 |
|---|---|---|
| 经历时间线 | 按章排列该角色的经历条目（summary / 关键台词原文 quotes / 做过的事 acted / 情绪变化 felt / 得失 got_lost / 未说出口的秘密 secrets） | 查看；AI 生成后修正；点击条目跳转对应章节正文 |
| 知识清单 | 该角色"知道 / 不知道"的信息项列表，可切换两种视图 | 手动调整知晓关系（如设定"他其实早就知道"） |
| 承诺清单 | 该角色立下的 flag（kind/content/状态），标注立下与预期回收章节 | 手动登记/标记回收 |
| 记忆摘要 | AI 按需生成该角色最近 N 章经历的一句话压缩概览 | 一键生成 |

> 关键约束：**经历条目中的 `quotes` 必须保存台词原文**（摘要是会失真的），正文生成时注入原文，防止 LLM 复述走样。

### 3.5 大纲构造（Outline Architect）

- 作品级 → 卷级两层大纲。
- **AI 能力**：
  - 由"主题 + 设定 + 核心角色"生成主线结构建议（如三幕式 / 起承转合 / 升级流曲线）；
  - 生成卷级大纲：每卷的目标、核心冲突、起止事件、卷末钩子；
  - 对大纲进行"爽点/泪点/悬念密度"评估并给出调整建议。
- 用户可在树形结构中自由增删改卷，AI 会基于修改后的大纲重新计算后续结构。

### 3.6 章节划分（Chapter Planner）

- 以卷为单位，将大纲拆解为章节计划，每章包含：章序、章标题（可 AI 拟题）、本章目标、预计字数、**视点角色（POV）**。
- **AI 能力**：
  - 根据卷的冲突节奏自动拆章（章节长度可按用户偏好 2000/3000/4000 字配置），并为每章指定 POV 角色；
  - 对既有章节计划做节奏检查（连续 3 章无冲突 → 提示插入冲突/转折）；
  - 支持"本章标题拟题"批量生成。

### 3.7 章节细纲（Chapter Beat Outline）

- 每章细纲为**场景级节拍列表**，节拍字段：场景序号、视点角色、地点、时间、事件（一句话）、情感走向、关键台词/道具/伏笔钩子、**涉及信息项**（本场景哪个角色知晓/不知晓什么）。
- **AI 能力**：
  - 依据本章目标与检索到的设定、**相关角色经历**生成 5~10 个节拍；
  - 支持"拉长"（节拍 → 多场景细化）与"压缩"（多节拍 → 大纲摘要）；
  - 检查节拍与设定库冲突、节拍间逻辑断裂、节拍与角色记忆冲突。

### 3.8 正文写作（Drafting）

- Markdown 富文本编辑器，支持按章编辑，实时字数统计与目标进度条。
- **生成配置（v1.3）**：每次生成前可查看/调整生成参数（默认取作品级配置，见 3.14）：目标字数、行文风格、视角（第一/第三人称）、节奏、对话密度、温度、记忆强度、质量模式（草稿/精修）。字数目标实时驱动进度条。
- **AI 能力**：
  - **续写**：从光标处续写（输入为前文 + 本节拍目标 + **该 POV 角色的经历档案**）；
  - **扩写**：对选中段落按细纲节拍扩写；
  - **多分支**：同一节拍给出 2~3 种走向供选择；
  - **润色**：风格统一、删冗余、增强画面感；
  - 流式输出（逐 token 上屏），可随时中断，中断后已生成部分可保留为草稿。
- **记忆约束（硬性）**：每次生成前注入 POV 角色经历档案与知识状态，并要求 LLM 遵守三条铁律：
  1. 角色言行必须与其经历档案连续——**不可否认或遗忘前文已发生之事、已说之话**；
  2. 角色只能说其知识状态内知晓的信息——**不可未卜先知**；
  3. 提及关键台词时沿用经历档案中的原文，而非改写。
- **设定沉淀触发（v1.3）**：用户接受生成内容时，自动检测其中涌现的新设定（地点/物品/规则/组织/人物等），弹出候选列表供勾选入库（见 3.12）；正文生成的 Prompt 中要求 LLM 对"新出现的专有设定"以 `{{设定标记}}` 标注，便于提取。
- 每章定稿后自动触发**记忆提取**（见 3.8.1）与章节摘要更新。

#### 3.8.1 记忆提取（Memory Extraction，每章定稿后后台执行）

```
① 剧情事件日志   从正文抽取 story_events（全知：发生了什么、谁参与、新信息、伏笔）
② 角色经历档案   按参与角色拆分生成 character_events（限知视角重述 + 台词原文引用）
③ 知识状态       检测新揭晓的信息项，更新 character_knowledge 知晓关系
④ 承诺跟踪       登记新 flag，推进既有 promises 状态（open→pending→fulfilled）
⑤ 章节摘要       生成/更新本章滚动摘要（仅作概览压缩，不替代①②③④）
```

- 提取结果默认自动写入（结构化 JSON 输出 + schema 校验），同时进入"记忆修正"面板供用户审阅修改；可在设置中切换为"先预览后应用"。
- 用户后续手改正文后，可对受影响章节**重新提取**，避免记忆与正文脱节。

### 3.9 修订与一致性检查（Revision & Consistency）

- **全局一致性扫描**（v1.1 扩展为六类问题）：

| 类型 | 检查内容 | 依据 |
|---|---|---|
| `setting` | 正文与世界观铁律冲突 | 设定库 canonical 条目 |
| `timeline` | 时间线矛盾（时间、季节、年龄、事件顺序） | 设定库 + 剧情事件日志 |
| `character` | 角色行为/性格不符角色卡 | 角色卡 |
| **`memory_conflict`** | **角色否认/遗忘前文已发生之事、已说之话** | **角色经历档案** |
| **`knowledge_leak`** | **角色说出/表现出本不该知道的信息** | **知识状态** |
| **`promise_break`** | **承诺/伏笔该回收未回收，或回收方式违背立约** | **承诺跟踪** |

- **逐条修复**：对扫描出的问题，LLM 给出"位置 + 问题 + 修改建议"三要素，用户可逐条应用或忽略；应用修改后自动触发对应章节的记忆重提取。
- **风格检查**：按作品风格指南检查文风漂移。

### 3.10 AI 交互范式（核心 UX 约定）

所有 AI 生成遵循统一四步循环：

```
① 生成  用户点击"AI 生成/续写/检查"
② 预览  LLM 流式输出到"建议草稿"面板，不直接改动正式内容
③ 编辑  用户可部分采纳、手动修改草稿
④ 接受  点击"应用"才写入正式数据（应用前可随时"丢弃"）
```

**交互执行路径（v1.2 明确）**：用户与 LLM 的交互不只有一种形态，按流程复杂度分两条路径执行，但对外都表现为上述四步循环：

| | 路径 A：单次直连 | 路径 B：多步工作流图 |
|---|---|---|
| 适用 | 一次 LLM 调用即可完成的功能：点子扩展/评估、世界观/大纲/细纲生成、续写、扩写、润色、对话模拟 | 多步骤、需状态传递与中间确认点的流程：**记忆提取、一致性扫描、批量章节生成**（进阶：章节全流程流水线） |
| 执行 | Service 层组装上下文 → LLM 客户端单次调用（流式/结构化）→ 预览 | LangGraph 图编排多个节点（LLM 调用、解析、校验、分支），见 6.7 |
| 确认点 | 由 UI 草稿面板手工实现 | 图中 `interrupt` 节点暂停执行、落盘 checkpoint，用户在草稿面板确认后 `resume` 继续 |
| 失败恢复 | 重试本次调用 | checkpoint 断点续跑，无需从头重来 |

**指令驱动形态（v1.3）**：除"按钮触发"外，新增**全局 AI 指令栏**（`Ctrl+J`）——用户不必先定位到具体条目，直接输入自然语言指令（如"把反派的身份改成主角失散多年的哥哥，并同步调整前 3 章伏笔"），由指令 Agent 自主完成"定位影响范围 → 产出修改计划 → 用户确认 → 执行修改 → 同步记忆与设定"。执行同样遵守"确认后写入"原则，详细见 3.11。

设计原则：
- **永不静默改写**：AI 不直接覆盖用户已确认的内容；
- **一切可追溯**：每条写入数据记录来源（人工/AI 生成 + 使用的模型与 Prompt 版本）；
- **可重试可回退**：生成失败可一键重试（断点续生成），数据表保留历史版本字段；
- **框架仅用于多步流程**：单次调用不走图，避免把简单功能卷入重框架（见 6.7）。

---

### 3.11 AI 指令驱动编辑（自然语言修改，v1.3）

**目标**：消除"用户必须先找到对应条目，才能让 AI 处理"的操作成本。用户用一句话描述意图，LLM 自主判断**需要修改哪些设定/角色/章节/记忆**，而不是用户手动逐个触发。

**指令示例**：

| 指令 | Agent 需要自主完成的事 |
|---|---|
| "把反派的身份改成主角失散多年的哥哥" | 定位角色卡、检查其经历档案与知识状态，列出需同步的伏笔与章节 |
| "给主角加一个金手指：每次受伤会愈合，代价是失去一段记忆" | 修改角色卡能力字段，检测与已有记忆冲突，新增规则设定 |
| "第 5 章节奏太拖，删掉茶馆闲聊，改成反派登场" | 重写细纲节拍、调整正文，联动更新该章记忆提取结果 |
| "把世界观的灵气复苏时间从 2077 提前到 2049" | 全库检索所有引用该时间的条目与正文，逐条列出待改项 |

**执行流程**（由 LangGraph 工作流图 `edit_command_workflow` 编排，见 6.7.5）：

```
用户输入指令（Ctrl+J 指令栏）
  │
  ├─ ① 解析与实体定位：识别指令涉及的 角色/章节/条目/信息项（实体消歧）
  ├─ ② 只读工具检索：读取相关设定、角色卡、经历档案、正文片段
  ├─ ③ 产出【修改计划】（结构化 JSON）：
  │      ·影响范围摘要（涉及哪些数据，为什么）
  │      ·逐条变更项（数据域 / 目标对象 / 原值摘要 / 新值 / 同步影响）
  │      ·需新增的设定建议（进入设定沉淀候选）
  ├─ ④ interrupt：用户在指令面板逐条勾选/编辑变更项后确认
  ├─ ⑤ 执行：按确认后的计划写入（写工具，仅操作被确认的变更项）
  └─ ⑥ 同步：变更摘要 → 受影响章节标记记忆需重提取 → 新设定进入沉淀候选
```

**安全约束**：
- 写操作一律以"修改计划"形式呈现，**逐条确认**后才执行；支持对单条变更项取消；
- 每类数据域执行前自动备份（历史版本字段），执行后可一键回退；
- 全部动作记入 `generation_logs`（模块 `command`），变更内容可追溯。

---

### 3.12 设定沉淀（Canon Capture，v1.3）

**问题**：正文/大纲创作时 LLM 必然"发明"设定库之外的新设定（新地点、新物品、新规则、新组织）。若不沉淀，后续生成会遗忘或再次发明出互相矛盾的版本。

**触发点**：
1. 用户接受任何 AI 生成的正文/大纲/细纲内容时（即时触发）；
2. 章节定稿流水线（随记忆提取一并执行）。

**流程**：

```
用户点击"应用"接受生成内容
  │
  ├─▶ 检测：LLM 从生成内容中提取"新出现的专有设定"（对照现有设定库去重）
  │        （生成 Prompt 要求 LLM 以 {{设定名}} 标记新设定，降低漏检）
  ├─▶ 候选列表：分类 / 标题 / 内容摘要 / 重要度 / 建议来源章节
  ├─▶ 用户勾选（可编辑摘要、修改分类、标记"铁律"）
  ├─▶ 确认 → 写入 world_entries（canonical 可选）
  └─▶ 联动：若新设定涉及秘密/真相（如"某地藏着主角身世的线索"），
      顺带建议登记 info_items 并更新相关角色知识状态
```

- 设置中可切换"始终确认"（默认，安全）或"自动沉淀低风险项"（如重要度 ≤2 且无冲突的条目直入，其余待确认）。
- 沉淀条目与普通条目同库（`world_entries`），立即可被后续生成检索到，避免遗忘。

---

### 3.13 写作教程知识库与基础提示词管理（v1.3）

**基础提示词（base_prompt）界面化可编辑**：

- 提示词分三级，均可在"提示词管理"页内嵌编辑：
  1. **全局基础提示词**（写作身份、通用铁律、记忆铁律）；
  2. **作品级提示词**（覆盖/追加，如本作品的文风基调）；
  3. **任务模板**（大纲/细纲/正文/记忆提取/一致性扫描等，含 `{{占位符}}`）。
- 编辑支持：语法高亮、`{{占位符}}` 说明、**版本历史**（version 字段 + 历史快照）、一键"恢复内置默认"。
- 所有修改即时生效并记录版本，写入 `generation_logs.prompt_key + version` 可追溯某次生成用的是哪个版本。

**教程知识库（Handbook）导入**：

- 导入方式：粘贴文本 / 导入 `.md`/`.txt` 文件 / 从 URL 抓取（可选功能）。
- 按分类组织（大纲、正文、角色、世界观、节奏与爽点、市场等，分类可自定义），分类决定注入时机。
- 长教程自动**切片**（按标题/段落）并建 FTS5 索引；生成时按当前任务类型检索相关切片，注入 prompt 的【写作技巧参考】区块（预算约 1k token，见 6.4）。
- 短教程（如 500 字内的核心方法论）可标记"整篇注入"，并入作品级提示词。
- 用户提供的示例（网文创作指南类网站）即典型用法：整站教程导入 → 正文生成时自动引用"章节节奏""对话技巧"等章节。
- 知识库全局可用（跨项目），也可绑定单个项目。

---

### 3.14 生成配置（Generation Profile，v1.3）

**两级配置**：全局默认（`app_settings`）+ 作品级（`projects.settings` JSON），作品级优先；每次生成前的参数面板可**单次覆盖**。

| 配置项 | 说明 | 默认 |
|---|---|---|
| 目标字数 | 每章目标字数 + 浮动范围（如 3000±10%） | 3000 |
| 行文风格 | 文风关键词/示例文本（对接 `style_guide`），如"网文爽快流/严肃文学/轻小说" | 作品级设置 |
| 视角 | 第一人称 / 第三人称（单视角 / 多视角） | 第三人称 |
| 节奏偏好 | 快节奏（多事件少描写）/ 慢热（重氛围） | 中 |
| 对话密度 | 高 / 中 / 低 | 中 |
| 环境与心理描写密度 | 高 / 中 / 低 | 中 |
| 章节标题风格 | 叙事式 / 悬念式 / 纯编号 | 叙事式 |
| 温度 | 0~1.5（创作类高、规划类低） | 0.8 |
| 记忆强度 | 严格 / 标准 / 宽松（见 6.4） | 标准 |
| 质量模式 | 草稿（低成本模型）/ 精修 | 草稿 |
| **模型档位（v1.6）** | 跟随路由表默认 / lite / standard / pro（单次覆盖，见 6.1.1） | 跟随默认 |

**注入方式**：组装器把生效配置渲染为 prompt 的【生成配置】区块；目标字数同时驱动编辑器的进度条与"本章字数不足时提醒续写"；**模型档位由路由解析器决定实际调用的模型**（见 6.1.1），不注入 prompt。

---

## 4. 系统架构

### 4.1 总体架构

```
┌────────────────────────── 桌面应用（PySide6） ──────────────────────────┐
│                                                                        │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌─────────────────┐  │
│  │ 点子/项目   │  │ 世界观/角色 │  │ 大纲/章节   │  │ 正文编辑器      │  │
│  │ 视图       │  │ +记忆视图   │  │ 树视图     │  │ + 建议草稿面板   │  │
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └────────┬────────┘  │
│        │               │               │                   │           │
│  ┌─────▼───────────────▼───────────────▼───────────────────▼───────┐  │
│  │                 Service 业务层（同步事务 + 事件总线）             │  │
│  │  IdeaSvc WorldSvc CharacterSvc OutlineSvc ChapterSvc DraftSvc    │  │
│  │  CheckSvc MemorySvc CanonCaptureSvc HandbookSvc CommandSvc       │  │
│  └───┬──────────────────┬───────────────────────────────────────────┘  │
│      │ 路径A：单次直连  │ 路径B：多步流程                              │
│      │（无编排层）       ▼                                            │
│      │          ┌──────────────────────────────────────────┐          │
│      │          │  Workflow 编排层（LangGraph，见 6.7）      │          │
│      │          │  StateGraph：节点=LLM调用/解析/校验/分支   │          │
│      │          │  interrupt：人机确认点（对应四步循环②③）   │          │
│      │          │  checkpointer：SQLite 断点续跑/历史回放    │          │
│      │          └──────────────────────────────────────────┘          │
│      └────────────────────┬───────────────────────────────────────────┘
│                           │（统一汇入 LLM 服务层）                      │
│  ┌─────▼──────────────┐   ┌────────▼─────────────────────────┐        │
│  │  DB 层 (SQLite)    │   │  LLM 服务层（异步）                 │        │
│  │  SQLAlchemy ORM    │   │  ├ 客户端抽象(OpenAI兼容)           │        │
│  │  FTS5 全文检索      │   │  ├ Prompt 模板引擎                 │        │
│  │  （设定/事件/经历） │   │  ├ 上下文组装器(Context Assembler) │        │
│  └────────────────────┘   │  │   含 记忆注入器(Memory Injector) │        │
│                           │  ├ 结构化输出解析(JSON)             │        │
│                           │  └ 任务队列/流式/取消               │        │
│                           └────────┬─────────────────────────┘        │
└────────────────────────────────────┼───────────────────────────────────┘
                                     │ HTTPS（OpenAI 兼容协议）
                          ┌──────────▼──────────┐
                          │  云端 LLM API        │
                          │  DeepSeek / 通义 /   │
                          │  OpenAI / 智谱 / Kimi│
                          └─────────────────────┘
```

### 4.2 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+ | 生态成熟、异步与桌面开发兼备 |
| GUI | PySide6 (Qt 6) | 桌面应用、原生体验、Tree/Table/Markdown 控件齐全、可打包 exe |
| 数据库 | SQLite + SQLAlchemy 2.0 | 单机单用户、零运维、事务可靠；ORM 便于演进 |
| 全文检索 | SQLite FTS5 | 设定/事件/经历检索 MVP 方案，免引入独立搜索引擎；后续可升级向量检索 |
| LLM 客户端 | `openai` SDK（兼容协议）+ `httpx` 流式 | 一套协议覆盖全部云端厂商，流式 SSE 支持良好 |
| 工作流编排 | **LangGraph**（仅多步流程使用，见 6.7） | `interrupt` 提供框架级人机确认点；`checkpointer`（SQLite）提供断点续跑与历史回放 |
| 异步 | `asyncio` + Qt 线程桥接（`qasync` 或 QThreadPool） | LLM 长请求不阻塞 UI，可中断 |
| 配置 | `tomllib` 读取 `config.toml` + 设置界面 | 模型/密钥/偏好集中管理，密钥不入库 |
| Markdown | `markdown-it-py` + QTextEdit 高亮编辑 | 正文以 Markdown 存储，兼顾可读与结构化 |
| 打包 | PyInstaller | 单机分发 |

### 4.3 模块划分

| 目录 | 职责 |
|---|---|
| `app/core/` | 配置、日志、事件总线、应用常量、异常体系 |
| `app/db/` | **数据库访问的唯一收口**：Session 工厂、SQLAlchemy 模型、仓储层（repositories/）、事务边界（unit_of_work）、迁移、FTS5 索引。**其他任何层禁止直接操作数据库**（见 4.4） |
| `app/llm/` | 客户端抽象、Prompt 模板注册表、上下文组装器（含记忆注入器）、JSON 解析、用量统计 |
| `app/services/` | 业务服务：Idea/World/Character/Outline/Chapter/Draft/Check/Memory/**CanonCapture**（设定沉淀）/**Handbook**（教程知识库）/**Command**（指令执行） |
| `app/workflows/` | 多步流程图定义（记忆提取/一致性扫描/批量生成/**指令执行 edit_command**）、LangGraph 封装层，对外暴露统一 `run_workflow(name, inputs)` 接口；业务层不直接依赖 LangGraph API |
| `app/tools/` | 指令 Agent 的工具注册表：只读工具（检索设定/正文/记忆）与写入工具（按确认计划执行），写入工具仅被工作流确认后调用，且**必须经仓储层**（见 4.4） |
| `app/workers/` | 后台生成任务（队列、进度、取消）、流式回调、记忆提取任务 |
| `app/ui/` | 主窗口、各模块视图（含角色记忆视图、事件日志视图）、AI 助手面板、设置页 |
| `data/` | SQLite 数据库文件与自动备份 |

### 4.4 数据库访问规范（Repository 模式，强制，v1.5）

**核心规则：所有数据库操作只允许出现在 `app/db/` 内；UI / Service / workflows / tools / workers / 脚本 等任何其他层，一律禁止直接执行 SQL 或 ORM 查询。**

```
┌─ 调用链（单向依赖，禁止反向/越层）───────────────────────────┐
│  UI 视图 / workers / 脚本        （禁止触碰 DB）             │
│      │ 调用                                              │
│      ▼                                                  │
│  Service 业务层                  （只允许调用仓储方法）      │
│      │ 调用                                              │
│      ▼                                                  │
│  Repository 仓储层（app/db/repositories/） ← 唯一数据库访问点│
│      │ 内部使用                                          │
│      ▼                                                  │
│  SQLAlchemy ORM / 原生 SQL / FTS5                        │
└──────────────────────────────────────────────────────────┘
```

**规则明细**：

1. **仓储层是唯一访问点**：所有 CRUD、查询、FTS5 检索、统计都封装在 `app/db/repositories/` 的 Repository 类中（`IdeaRepo` / `WorldEntryRepo` / `CharacterRepo` / `ChapterRepo` / `MemoryRepo` / `CanonRepo` / `HandbookRepo` / `PromptRepo` / `GenerationLogRepo` 等）。Repository 对外只暴露语义化方法（如 `get_chapter_with_memory(chapter_id)`），内部才允许使用 ORM/SQL。
2. **禁止清单**（违反视为代码评审不通过）：
   - `app/ui/` 任何文件不得 import Session/Engine，不得出现查询语句；
   - `app/services/` 不得调用 `session.query(...)` / `session.execute(...)` / `text(...)`，只能调用 Repository 方法；
   - `app/workflows/` 与 `app/tools/`（Agent 写入工具）同样只经 Repository 读写数据，不得内嵌 SQL；
   - 脚本（测试/运维）不得直连数据库——一律通过 Service/Repository 暴露的接口；
   - `app/db/migrations.py` 是**唯一允许裸 SQL/DDL** 的文件（建表、索引、迁移）。
3. **事务边界（Unit of Work）**：跨表写操作（如记忆提取 commit、指令执行批量变更）由 Service 层通过 `UnitOfWork` 上下文管理"提交/回滚"；Repository 方法只做单次原子操作，**不得自行 commit**；一个事务内全部成功才提交。
4. **会话管理**：Session 由 `app/db/session.py` 统一创建并按需注入（依赖注入 / 上下文管理器），禁止各层自行 `Session()`。
5. **返回对象**：Repository 返回领域模型或纯 DTO；禁止把绑定 session 的对象直接交给 UI 层长期持有（防 DetachedInstance 错误）。
6. **只读方法**：查询类方法统一命名前缀 `get_` / `search_` / `count_`，写方法用 `create_` / `update_` / `delete_`，便于评审与检索定位。
7. **测试隔离**：`tests/` 使用内存 SQLite 或 Repository mock，不触碰真实库文件。

**配套目录（app/db/ 展开）**：

```
app/db/
├── session.py          # Session 工厂 / 上下文（唯一会话入口）
├── models.py           # SQLAlchemy 模型（表映射，见 5.3）
├── migrations.py       # 迁移脚本（唯一允许裸 DDL 的文件）
├── fts.py              # FTS5 虚拟表创建与索引维护
├── unit_of_work.py     # 事务边界（提交/回滚上下文）
└── repositories/       # ★ 仓储层：唯一数据库访问点
    ├── base.py         # 通用 CRUD 基类
    ├── idea_repo.py / world_repo.py / character_repo.py
    ├── chapter_repo.py / memory_repo.py / canon_repo.py
    ├── handbook_repo.py / prompt_repo.py / generation_log_repo.py
```

---

## 5. 数据模型设计

### 5.1 数据组织层级（一本长篇 = 一个项目，v1.4 明确）

```
┌─ 全局层（不区分项目，跨书共享）──────────────────────────────┐
│  ·模型配置：API Key / 模型 / 温度（config.toml）              │
│  ·全局基础提示词（prompt_templates.scope='global'）           │
│  ·教程知识库（handbook_docs.project_id 可空=全局，或绑定项目） │
│  ·应用级偏好：草稿/精修默认、记忆强度默认、沉淀自动档位        │
└─────────────────────────────────────────────────────────────┘
┌─ 项目层（一本长篇 = 一个项目，全部数据经 project_id 关联）─────┐
│  ·项目信息：名称/类型/一句话梗概/主题/文风指南                  │
│  ·作品级生成配置：目标字数/风格/视角/节奏（projects.settings）  │
│  ·创作流水线数据：点子 → 世界观 → 角色（含记忆）                │
│                    → 大纲 → 卷 → 章节（细纲+正文）             │
│  ·三层记忆：事件日志 / 角色经历 / 知识状态 / 承诺               │
│  ·设定沉淀候选（proposed_entries，确认后转 world_entries）     │
│  ·作品级提示词覆盖（prompt_templates.scope='project'）         │
│  ·AI 调用留痕（generation_logs）                              │
└─────────────────────────────────────────────────────────────┘
```

**多本书管理**：应用首页为"项目列表"（新建 / 打开 / 归档 / 删除），一次可打开一个项目进入工作台；数据按项目物理隔离，互不干扰。

> **系列作品复用：暂不处理（v1.4 决策）**。写续集/同世界观新书时的世界观继承（设定导出导入、项目模板、共享设定库）本次不纳入设计范围，后续如有需求再评估。

### 5.2 ER 总览

```
Project 1 ──┬── * Idea
            ├── * WorldCategory 1 ── * WorldEntry
            ├── * Character ── * CharacterRelation（自关联）
            ├── * TimelineEvent
            ├── * Arc 1 ── * Chapter（含细纲 beats_json + 正文 content）
            ├── * StoryEvent（全知事件日志，挂接 Chapter）
            ├── * CharacterEvent（限知经历档案，挂接 Character + Chapter）
            ├── * InfoItem ── * CharacterKnowledge（角色-信息知晓关系）
            ├── * Promise（承诺/伏笔跟踪）
            ├── * ProposedEntry（设定沉淀候选，确认后转 WorldEntry）
            └── * GenerationLog（每次 AI 调用留痕）
全局：PromptTemplate（三级，可编辑）、AppSetting、HandbookDoc ── * HandbookChunk（教程知识库）
```

### 5.3 表结构定义（SQLite DDL 草案）

```sql
-- 项目
CREATE TABLE projects (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,
  genre       TEXT,               -- 类型/题材
  logline     TEXT,               -- 一句话梗概
  theme       TEXT,               -- 主题
  style_guide TEXT,               -- 文风指南（供正文生成注入）
  status      TEXT DEFAULT 'active',   -- active / archived
  settings    TEXT,               -- JSON：生成配置（见 3.14：目标字数/风格/视角/节奏等）
  created_at  TEXT NOT NULL,
  updated_at  TEXT NOT NULL
);

-- 点子
CREATE TABLE ideas (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER REFERENCES projects(id),
  title      TEXT,
  summary    TEXT,                -- 点子内容
  tags       TEXT,                -- 逗号分隔
  status     TEXT DEFAULT 'draft',-- draft/expanded/selected/discarded
  eval_json  TEXT,                -- AI 四维评估结果
  created_at TEXT NOT NULL
);

-- 世界观分类与条目
CREATE TABLE world_categories (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER REFERENCES projects(id),
  name       TEXT NOT NULL,
  sort_order INTEGER DEFAULT 0
);

CREATE TABLE world_entries (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  category_id INTEGER REFERENCES world_categories(id),
  title       TEXT NOT NULL,
  content     TEXT NOT NULL,
  tags        TEXT,
  importance  INTEGER DEFAULT 3,  -- 1~5
  canonical   INTEGER DEFAULT 0,  -- 1=铁律，一致性校验强制比对
  updated_at  TEXT NOT NULL
);
-- 为 world_entries 建 FTS5 虚拟表用于检索注入

-- 角色
CREATE TABLE characters (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  name        TEXT NOT NULL,
  aliases     TEXT,
  role_type   TEXT,               -- protagonist/antagonist/support/major/minor
  profile     TEXT,               -- JSON：性格/外貌/背景/动机/能力/说话风格
  arc         TEXT,               -- JSON：成长弧光起点-终点
  status      TEXT DEFAULT 'alive',
  created_at  TEXT NOT NULL,
  updated_at  TEXT NOT NULL
);

CREATE TABLE character_relations (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  from_id     INTEGER REFERENCES characters(id),
  to_id       INTEGER REFERENCES characters(id),
  relation    TEXT,               -- 师徒/宿敌/恋人/盟友...
  description TEXT
);

-- 卷与章节
CREATE TABLE arcs (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  title       TEXT,
  seq         INTEGER,            -- 卷序号
  goal        TEXT,               -- 本卷目标
  conflict    TEXT,               -- 核心冲突
  resolution  TEXT,               -- 收束方式
  summary     TEXT,
  status      TEXT DEFAULT 'planned'  -- planned/outlined/writing/done
);

CREATE TABLE chapters (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id     INTEGER REFERENCES projects(id),
  arc_id         INTEGER REFERENCES arcs(id),
  seq            INTEGER,         -- 章序号（全书连续）
  title          TEXT,
  pov_char_id    INTEGER REFERENCES characters(id),  -- 本章视点角色（记忆注入优先级依据）
  objective      TEXT,            -- 本章目标
  beats_json     TEXT,            -- 细纲节拍列表 JSON
  outline_status TEXT DEFAULT 'none',  -- none/beats/done
  content        TEXT,            -- 正文 Markdown
  content_status TEXT DEFAULT 'empty', -- empty/draft/first_draft/polished
  word_count     INTEGER DEFAULT 0,
  summary        TEXT,            -- 本章摘要（滚动摘要源）
  memory_status  TEXT DEFAULT 'none',  -- none/pending/extracted/needs_refresh
  updated_at     TEXT NOT NULL
);

-- ================= 三层记忆体系（v1.1 新增） =================

-- 第 1 层：剧情事件日志（全知视角）
CREATE TABLE story_events (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  chapter_id  INTEGER REFERENCES chapters(id),
  seq         INTEGER,            -- 章内事件顺序
  title       TEXT,               -- 事件名
  summary     TEXT,               -- 一句话事件描述（发生了什么）
  location    TEXT,
  time_marker TEXT,
  char_ids    TEXT,               -- 参与角色 id 列表
  info_gained TEXT,               -- 本事件暴露的新信息（info_items id 列表）
  promises    TEXT,               -- 本事件立下/推进的 flag（promises id 列表）
  updated_at  TEXT NOT NULL
);

-- 第 2 层：角色经历档案（限知视角）
CREATE TABLE character_events (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id   INTEGER REFERENCES projects(id),
  character_id INTEGER REFERENCES characters(id),
  chapter_id   INTEGER REFERENCES chapters(id),
  event_ref    INTEGER,           -- 关联 story_events.id（可选，保持两视图可追溯）
  importance   INTEGER DEFAULT 3, -- 1~5，检索加权（大战/誓言/转折=5，日常=1~2）
  summary      TEXT,              -- 该角色视角下"发生了什么"
  quotes       TEXT,              -- 关键台词【原文】，防止复述走样（必须保真）
  acted        TEXT,              -- 该角色做过的事 / 做出的决定
  felt         TEXT,              -- 情绪与心理变化
  got_lost     TEXT,              -- 获得/失去：物品、能力、伤势、关系变化
  secrets      TEXT,              -- 本角色知道但未说出口的秘密
  updated_at   TEXT NOT NULL
);
-- 索引：(project_id, character_id, chapter_id)；(project_id, chapter_id)

-- 第 3 层 a：信息项（秘密/真相/计划）
CREATE TABLE info_items (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  title       TEXT,               -- 如"主角的身世""反派真实身份"
  content     TEXT,
  status      TEXT DEFAULT 'hidden'  -- hidden（读者未知）/ partial / revealed
);

-- 第 3 层 b：角色-信息知晓关系（知识状态）
CREATE TABLE character_knowledge (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id      INTEGER REFERENCES projects(id),
  character_id    INTEGER REFERENCES characters(id),
  info_item_id    INTEGER REFERENCES info_items(id),
  knows           INTEGER DEFAULT 0,   -- 1=知道；0=不知道（两态必有一行，显式记录"不知道"）
  learned_chapter INTEGER,             -- 从哪一章开始知道（用于时间衰减检索）
  updated_at      TEXT NOT NULL
);

-- 第 3 层 c：承诺/伏笔跟踪
CREATE TABLE promises (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id   INTEGER REFERENCES projects(id),
  kind         TEXT,               -- promise(承诺)/foreshadow(伏笔)/cliffhanger(悬念)
  content      TEXT,               -- "主角承诺为小师妹报仇"
  by_char_id   INTEGER REFERENCES characters(id),  -- 相关角色
  made_chapter INTEGER,            -- 立下的章节
  due_chapter  INTEGER,            -- 预期回收章节（可空=不定期）
  status       TEXT DEFAULT 'open',-- open / pending / fulfilled / dropped
  resolution   TEXT,               -- 回收方式描述
  updated_at   TEXT NOT NULL
);

-- 时间线（可选，用于历史/编年体作品）
CREATE TABLE timeline_events (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id   INTEGER REFERENCES projects(id),
  title        TEXT,
  time_marker  TEXT,              -- "纪元 537 年·春"
  description  TEXT,
  char_ids     TEXT,              -- 涉及角色 id 列表
  entry_ids    TEXT               -- 涉及设定条目 id 列表
);

-- AI 调用留痕（成本/可追溯/断点续生成）
CREATE TABLE generation_logs (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER REFERENCES projects(id),
  module      TEXT,               -- idea/world/character/outline/chapter/draft/check/memory
  model       TEXT,
  prompt_key  TEXT,               -- 使用的 Prompt 模板 key
  in_tokens   INTEGER, in_chars   INTEGER,
  out_tokens  INTEGER, out_chars  INTEGER,
  status      TEXT,               -- ok/failed/cancelled
  created_at  TEXT NOT NULL
);

-- 全局配置与 Prompt 模板（v1.3：scope 支持三级提示词层级）
CREATE TABLE app_settings (key TEXT PRIMARY KEY, value TEXT);
CREATE TABLE prompt_templates (
  key      TEXT PRIMARY KEY,
  scope    TEXT DEFAULT 'global',  -- global（全局基础）/ project（作品级覆盖）/ task（任务模板）
  project_id INTEGER,              -- scope=project 时指定项目
  category TEXT,                   -- 对应模块
  name     TEXT,
  template TEXT,                   -- 含 {{占位符}} 的模板文本
  version  INTEGER DEFAULT 1,
  updated_at TEXT
);
CREATE TABLE prompt_versions (      -- 版本历史，支持回滚/恢复默认
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  key        TEXT,
  version    INTEGER,
  template   TEXT,
  created_at TEXT
);

-- ================= v1.3 新增：设定沉淀候选 =================

CREATE TABLE proposed_entries (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id   INTEGER REFERENCES projects(id),
  source_chapter INTEGER,          -- 来源章节（可空）
  source_module TEXT,              -- idea/world/outline/chapter/draft/command
  source_snippet TEXT,             -- 出处原文片段
  title        TEXT,
  content      TEXT,
  category     TEXT,               -- 建议分类（地理/规则/组织/物品...）
  importance   INTEGER DEFAULT 3,
  status       TEXT DEFAULT 'pending',  -- pending / accepted / rejected
  created_at   TEXT NOT NULL
);
-- accepted 后：写入 world_entries（canonical 可选），并回填关联

-- ================= v1.3 新增：教程知识库 =================

CREATE TABLE handbook_docs (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id  INTEGER,             -- NULL=全局可用
  title       TEXT NOT NULL,
  category    TEXT,                -- 大纲/正文/角色/世界观/节奏/市场...
  source_type TEXT,                -- paste / file / url
  content     TEXT NOT NULL,       -- 原文
  enabled     INTEGER DEFAULT 1,
  created_at  TEXT NOT NULL
);

CREATE TABLE handbook_chunks (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  doc_id     INTEGER REFERENCES handbook_docs(id),
  seq        INTEGER,
  title      TEXT,                 -- 切片标题（按标题/段落切）
  content    TEXT NOT NULL
);
-- 为 handbook_chunks 建 FTS5 虚拟表（教程检索注入）
```

**设计要点**：
- **三层记忆分层存储**：全知事件（`story_events`）与限知经历（`character_events`）分离，避免"事件"与"角色的主观记忆"混为一谈；通过 `event_ref` 双向可追溯，正文/事件/经历互相跳转。
- `character_events.quotes` 保存**台词原文**，这是防止 LLM 复述走样的关键——摘要必然失真，原文引用才可靠。
- `character_knowledge` 对每个"角色×信息项"显式记录知道/不知道两态，确保"不知道"也是被建模的事实。
- 细纲与角色卡用 JSON 列存储结构化数据，避免过度建表；记忆类表单独建 FTS5 索引供检索。
- **生成配置**存于 `projects.settings`（作品级）与 `app_settings`（全局默认），每次生成时组装器读取生效配置渲染为【生成配置】区块。
- **提示词三级层级**（global/project/task）经 `prompt_templates.scope` 区分，`prompt_versions` 保存历史快照支持回滚；教程知识库（`handbook_docs/chunks`）独立于提示词，按任务检索注入，不污染模板本身。
- 所有 AI 生成内容在写入前都经过"建议草稿"层，正式表只存用户确认后的数据；记忆提取默认自动写入，但提供"记忆修正"入口。

---

## 6. LLM 集成设计

### 6.1 LLM 抽象层

```python
@dataclass
class LLMConfig:
    base_url: str      # 如 https://api.deepseek.com/v1
    api_key: str       # 存于 config.toml / 系统钥匙串，不入库
    model: str         # deepseek-chat / qwen-max / gpt-4o ...
    temperature: float = 0.7
    max_tokens: int = 4096

class LLMClient(Protocol):
    async def chat(self, messages, config, *, stream=False) -> AsyncIterator[str]: ...
    async def chat_structured(self, messages, config, schema) -> dict: ...  # JSON 模式
```

- 全部云端厂商均暴露 OpenAI 兼容 `/chat/completions`，客户端只依赖 `openai` SDK 一个实现。
- 统一错误处理：限流 429 退避重试、网络重试（指数退避）、超时中断、错误消息上抛 UI；失败时按 6.1.2 降级策略处理。

#### 6.1.1 模型路由（Model Routing，v1.6）

**目标**：不同任务按"难度 / 重要程度 / 质量要求"使用不同档位的模型——高价值任务用强模型保证质量，高吞吐结构化任务用低成本模型控制成本，并支持不同档位落在**不同厂商**的模型上。

**模型档位（Tier）**：每个 Provider 提供三档模型映射：

| 档位 | 定位 | 典型模型示例 |
|---|---|---|
| `lite` | 高吞吐、低重要性、结构化任务 | deepseek-chat / gpt-4o-mini / qwen-turbo |
| `standard` | 常规生成任务（默认档） | deepseek-chat / gpt-4o / qwen-plus |
| `pro` | 高价值、需准确性/创造力的任务 | deepseek-reasoner / gpt-4o / qwen-max |

**路由依据（双维度）**：任务类型（module + prompt_key）+ 质量模式（草稿/精修，见 3.14）。内置路由表（可在设置页编辑）：

| 任务 | 默认档位 | 理由 |
|---|---|---|
| 记忆提取 / 设定提取 / 章节摘要 | `lite` | 结构化、低创造力、吞吐大 |
| 点子扩展评估 / 世界观生成 / 细纲生成 / 章节拆解 / 续写 / 扩写 / 润色 | `standard` | 常规创作任务 |
| 大纲结构生成 | `pro` | 影响全书结构 |
| 正文正式生成（质量=精修） | `pro` | 质量敏感 |
| 正文草稿（质量=草稿） | `lite` | 批量初稿可降档 |
| 一致性扫描 | `pro` | 准确性关键 |
| 指令执行（修改计划） | `pro` | 强模型低温度，防止误改 |

**覆盖优先级**（高→低）：单次操作指定（生成参数面板"模型档位"）→ 项目级覆盖（`projects.settings`）→ 全局默认（`app_settings`）→ 内置路由表默认。

**配置示例（config.toml）**：

```toml
[llm.providers.deepseek]
base_url = "https://api.deepseek.com/v1"
api_key = "sk-..."
models = { lite = "deepseek-chat", standard = "deepseek-chat", pro = "deepseek-reasoner" }

[llm.providers.openai]                # 可选：第二厂商，Pro 档可用更强模型
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
models = { lite = "gpt-4o-mini", standard = "gpt-4o", pro = "gpt-4o" }

[llm.routing]
default_provider = "deepseek"         # 未指定厂商时的默认
default_tier     = "standard"
auto_downgrade   = true               # Pro 失败/限流时自动降级 standard 并提示
```

**路由解析（伪代码）**：

```python
def resolve_model(task: TaskSpec, override: Tier | None) -> ModelSpec:
    tier = (override                    # ① 单次覆盖
            or task.tier                # ② 任务内置档位（按路由表）
            or project_override_tier    # ③ 项目级覆盖
            or global_default_tier      # ④ 全局默认
            or "standard")
    provider = task.provider or routing.default_provider
    return providers[provider].models[tier]
```

**降级与升级**：
- `auto_downgrade=true`：`pro` 调用失败（限流/超时/超预算）→ 自动降级 `standard` 重试一次并提示用户；
- 用户对 `lite` 结果不满意 → 可一键"用更高档位重新生成"（UI 保留档位切换）。

**成本联动**：`generation_logs` 记录每次实际使用的 `provider + tier + model`，用量面板按档位单价分别统计（见 6.6）。

### 6.2 Prompt 模板管理（v1.3：界面化编辑 + 三级层级）

- 内置模板（随应用分发，可被用户覆盖），存于 `prompt_templates` 表，**三级层级**：

| scope | 用途 | 覆盖关系 |
|---|---|---|
| `global` | 全局基础提示词（写作身份、通用铁律、三条记忆铁律） | 所有任务 |
| `project` | 作品级覆盖/追加（本作品文风基调、特殊规则） | 覆盖 global 同名项 |
| `task` | 各任务模板（大纲/细纲/正文/记忆提取/一致性扫描/设定提取/指令执行等） | 含 `{{占位符}}` |

- **界面化编辑**：所有模板在"提示词管理"页内嵌编辑（语法高亮、占位符说明）；每次保存写入 `prompt_versions` 历史快照，可**回滚**或一键"恢复内置默认"。
- 模板采用 `{{占位符}}` 语法，渲染时由上下文组装器填充；每个模板带 `version`，写入 `generation_logs.prompt_key + version` 实现可追溯。
- **教程注入**：教程知识库（`handbook_chunks`）按任务类型检索相关切片，作为【写作技巧参考】区块注入任务 prompt（见 6.4 预算），与模板本身解耦。

示例（世界观条目生成模板）：

```
你是资深世界观设计师。请基于以下项目信息与点子，生成一份{分类}设定条目。
要求：内容具体可执行，避免空泛；与既有铁律设定不冲突；语言精炼。

【项目类型】{{genre}}
【点子】{{logline}}
【相关既有设定】
{{related_entries}}
【输出格式】严格 JSON：{"title": "...", "content": "...", "tags": [...], "importance": 1-5}
```

### 6.3 结构化输出

- 规划/提取类任务（生成细纲、评估点子、拆章、一致性扫描、**记忆提取、设定提取、指令修改计划**）要求 LLM 返回**严格 JSON**，客户端用 `response_format={"type":"json_object"}`（或厂商 JSON 模式）请求，服务端做 schema 校验，失败自动重试一次。
- 正文类任务使用纯文本流式输出，不做 JSON 包裹，保证 token 效率与可读性。

### 6.4 上下文组装策略（核心设计）

长篇小说正文动辄数十万字，无法整体进上下文。采用 **"分层 + 检索 + 预算裁剪"** 的组装器，其中**记忆注入器（Memory Injector）**负责把角色经历、知识状态、承诺状态按"视点优先 + 检索命中"拼入 prompt。

```
组装器输入：任务类型 + 当前对象（章节/角色/条目）+ 设定库 + 记忆库 + 已写正文
组装器输出：system + user 消息（按 token 预算裁剪）

预算分配（以 32k 输入窗口为例）：
  system 写作规则/文风指南                  ~2k   (6%)
  【生成配置】目标字数/视角/节奏等           ~0.5k (2%)   ← v1.3 新增
  【写作技巧参考】教程知识库检索 top-2       ~1k   (3%)   ← v1.3 新增，按任务类型
  当前卷大纲 + 本章细纲                    ~3k  (10%)
  检索注入 世界观条目 top-3                ~2k   (6%)
  出场角色卡（POV 角色优先）               ~3k  (10%)
  ★ POV 角色经历档案 top-8                ~4k  (13%)   ← 视点记忆，优先级最高
  ★ 其他角色相关经历 top-3                ~2k   (6%)   ← 与当前场景相关的他人经历
  ★ 知识状态（涉及信息项 + 谁知晓/不知晓）  ~1k   (3%)   ← 限知约束
  ★ 相关历史事件检索 top-3                 ~2k   (6%)   ← 与当前细纲/地点/人物相关的往事
  ★ 未回收承诺/伏笔提醒（与本章相关）       ~0.5k (2%)   ← flag 推进/回收提示
  前情滚动摘要                             ~3k  (10%)
  最近 1~2 章正文                          ~4k  (13%)
  余量（提示词与输出）                     ~4.5k(14%)
```

**教程检索策略**（Handbook Injector，v1.3）：按当前任务类型（正文/大纲/细纲/角色）检索 `handbook_chunks`（FTS5，词频+分类过滤），取 top-2 块约 1k token 注入【写作技巧参考】；短教程（<800 字）可标记整篇注入。裁剪时教程区块**最先被丢弃**（属增强项，非必需）。

**记忆检索策略**（Memory Injector）：

| 记忆源 | 检索规则 | 排序加权 |
|---|---|---|
| POV 角色经历档案 | 取当前 POV 角色的 `character_events` | 时间近 × 1.2，importance 高 × 1.5，与细纲关键词 FTS5 命中 × 2.0 |
| 其他角色经历 | 出场角色各自的经历，仅取与当前场景相关的 top-3 | FTS5 命中为主 |
| 相关历史事件 | `story_events` 中与当前地点/角色/物品/关键词相关的事件 | FTS5 命中 + 时间近 |
| 知识状态 | 当前细纲标注的"涉及信息项" + 出场角色的知晓关系 | 按场景优先级 |
| 承诺/伏笔 | `promises` 中 made_chapter ≤ 当前章 且 status ∈ {open, pending}，与本章角色相关 | 相关性 + 到期临近 |

**滚动摘要**：每章定稿后生成 300~500 字章节摘要，每卷生成卷摘要；正文生成时按"卷摘要 + 近 3 章摘要 + 最近 1~2 章正文"组装前情概览。**滚动摘要只用于"概览"，细节记忆一律由经历档案/事件检索提供**——这是 v1.1 的核心修正：不再让有损摘要承担"记忆"职责。

**裁剪规则**：超预算时按优先级逆序丢弃（先丢旧正文 → 再丢次要世界观设定 → 再丢非 POV 角色经历），**POV 经历档案与知识状态不可裁剪**（可压缩为浓缩版而非丢弃），并记录实际用量供状态栏展示。

**记忆强度开关**：设置页提供"记忆强度"三档（严格：全部注入+铁律约束 / 标准：POV 全量+他人 top-3 / 宽松：仅 POV top-3），供不同写作场景（精修 vs 快速草稿）调整。

### 6.5 流式输出与任务取消

- 所有长生成（正文、长建议）走 SSE 流式：Qt 主线程通过信号槽接收增量文本，逐段上屏。
- 每个生成任务有唯一 `task_id`，取消通过 `asyncio.Task.cancel()` + 关闭 HTTP 流；已生成内容保留在草稿面板，可手动另存。
- 后台任务并发上限默认 1（受单账号 API 限流约束），可配置为串行队列。

### 6.6 成本控制

- 每次调用写入 `generation_logs`（含实际 `provider + tier + model`），设置页按档位单价分别展示累计 token / 估算费用。
- 单次生成前在 UI 上预估输入 token 数（客户端近似 tokenizer），并显示本次将使用的档位与实际模型。
- 提供"草稿质量/精修质量"两级模式：草稿模式自动落到 `lite` 档，精修落到 `pro` 档（见 6.1.1）；记忆提取/设定提取默认走 `lite` 档（结构化任务对创造力要求低）。

### 6.7 工作流编排层（LangGraph，仅多步流程）

#### 6.7.1 定位与边界

用户与 LLM 的交互分两条执行路径（见 3.10）：**单次直连**（占绝大多数功能）与**多步工作流图**。引入 LangGraph **只用于后者**——单次调用走图会徒增重依赖与调试成本，属于过度设计。

```
路径 A（直连，占 ~75%）：Service → LLM 客户端
   点子扩展/评估、世界观生成、大纲生成、细纲生成、续写、扩写、润色、对话模拟、单章摘要

路径 B（工作流图，占 ~25%）：Service → Workflow 编排层（LangGraph）→ LLM 服务层
   记忆提取（memory_extract_workflow）      —— 5 步：事件→经历→知识→承诺→摘要
   一致性扫描（consistency_scan_workflow）  —— 分批取正文 → 对照设定/记忆 → 汇总报告
   ★ 指令执行（edit_command_workflow）      —— 自然语言指令 → 定位 → 修改计划 → 确认 → 执行（v1.3，见 6.7.5）
   ★ 设定沉淀（canon_capture_workflow）     —— 生成内容 → 新设定检测去重 → 候选列表 → 确认入库（v1.3）
   批量章节生成（batch_draft_workflow，进阶）—— 多章初稿 + 逐章记忆提取
   （进阶）章节全流程流水线（chapter_pipeline_workflow）
       —— 细纲确认 → 正文生成 → 记忆提取 → 摘要，链路上的确认点全部用 interrupt 表达
```

#### 6.7.2 与四步循环的对应（interrupt 机制）

LangGraph 的 `interrupt` 节点正是 3.10 四步循环中"②预览 / ③编辑"的框架级实现：

```
图执行到"生成结果"节点 → 调用 interrupt(state) 暂停
  → checkpoint 落盘（SQLite，记录全部中间状态）
  → 结果流入 UI 建议草稿面板（预览）
  → 用户确认/修改后点击"应用"
  → 图从断点 resume，携带用户的修正继续后续节点
```

收益：多步流程的"暂停-确认-继续"不再需要手写状态机；中断后即使应用退出，checkpoint 仍在，下次启动可恢复。

#### 6.7.3 记忆提取图示例（memory_extract_workflow）

```
                    ┌───────────────┐
   章节定稿 ───────▶│ load_chapter   │ 读取正文/细纲/出场角色
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ extract_events│ LLM 结构化：story_events
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐   ┌──────────────┐
                    │ per_char_mem  │──▶│ validate      │ JSON schema 校验
                    │ (循环 角色)    │   └──────┬───────┘
                    └───────┬───────┘          │ 失败 → 重试(限1次)
                            ▼                  ▼
                    ┌───────────────┐   ┌──────────────┐
                    │ update_know   │   │ update_prom  │
                    │ 知识状态更新   │   │ 承诺状态推进  │
                    └───────┬───────┘   └──────┬───────┘
                            └────────┬─────────┘
                                     ▼
                            ┌───────────────┐
                            │ gen_summary   │ 章节滚动摘要
                            └───────┬───────┘
                                    ▼
                            ┌───────────────┐
                            │ interrupt     │ ★ 用户审阅"记忆修正"面板
                            └───────┬───────┘
                                    ▼
                            ┌───────────────┐
                            │ commit        │ 写入 DB，memory_status='extracted'
                            └───────────────┘
```

#### 6.7.4 封装原则与 Qt 集成

- **封装隔离**：`app/workflows/` 内部使用 LangGraph，对外仅暴露 `run_workflow(name, inputs) -> WorkflowHandle`（含进度、事件流、`resume()` 接口）。Service 层与 UI 层**不直接接触 LangGraph API**，将来可无痛替换为自研轻量编排器（`graphlib` / `transitions`）。
- **流式集成**：LangGraph 的 `astream` 事件经 `qasync` 桥接为 Qt 信号槽，逐节点进度与 LLM 流式文本上屏。
- **checkpointer**：使用 LangGraph 的 SQLite checkpointer（独立于主库的 `data/workflow_state.db`），仅存图运行态，不存业务数据。
- **中断恢复**：UI 打开时扫描未完成的 checkpoint，提示用户"存在未完成的任务，是否恢复"。
- **版本锁定**：LangGraph/LangChain 迭代快，`pyproject.toml` 锁定精确版本，升级走专门验证。

#### 6.7.5 指令执行图示例（edit_command_workflow，v1.3）

Agent 形态（ReAct 风格：Reason + 工具调用），工具分**只读**与**写入**两类；写入工具只在用户确认修改计划后经图内 `commit` 节点调用：

```
                    ┌───────────────┐
   用户指令(Ctrl+J)─▶│ parse_intent  │ 解析意图 + 实体消歧（角色/章节/条目/信息项）
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ tool_search   │ 只读工具循环：检索设定/角色卡/经历/正文/知识状态
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ build_plan    │ LLM 产出【修改计划】JSON（影响范围+逐条变更项+新增设定建议）
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ interrupt     │ ★ 指令面板逐条勾选/编辑变更项，确认后 resume
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ tool_apply    │ 写入工具：仅执行被确认的变更项（每类数据域先备份）
                    └───────┬───────┘
                            ▼
                    ┌───────────────┐
                    │ summarize     │ 变更摘要 → 受影响章节标记记忆需重提取
                    │ + sync        │ → 新设定转入设定沉淀候选 → 记 generation_logs
                    └───────────────┘
```

- 工具注册表（`app/tools/`）：`search_world` / `search_character` / `search_memory` / `read_chapter`（只读）；`apply_world_edit` / `apply_character_edit` / `apply_chapter_edit` / `apply_beat_edit`（写入，仅确认后调用）。
- 实体消歧失败（指令中角色/章节指代不明）时，`parse_intent` 输出待澄清问题，**先 interrupt 向用户提问**，而不是猜。
- 指令执行默认走**强模型 + 低温度**（规划类），确保修改计划的准确性。

---

## 7. 用户界面设计

### 7.1 主窗口布局

```
┌────────────────────────────────────────────────────────────────┐
│ 菜单栏  文件/编辑/项目/AI工具/记忆库/教程库/设置                  │
│ 工具栏  [档位▾:自动/Lite/Std/Pro] [AI生成] [续写] [记忆提取] [一致性检查] [备份] │
│        [AI指令栏: 输入自然语言指令…                    ▶执行 Ctrl+J]│
├──────────────┬────────────────────────────────┬────────────────┤
│ 项目导航树    │  主内容区（标签页）              │  AI 助手面板    │
│ (左侧固定)    │                                │ (右侧可折叠)    │
│ ◆ 项目名      │  ┌──────────────────────────┐ │  ① 生成任务卡   │
│  ├ 仪表盘     │  │ 章节 12｜标题｜目标｜进度  │ │   ·状态/进度条  │
│  ├ 点子       │  │ ┌──────────────────────┐ │ │   ·中断按钮    │
│  ├ 世界观     │  │ │ 正文编辑器(Markdown)  │ │ │  ② 建议草稿区  │
│  │ └设定沉淀  │  │ │ 流式内容实时上屏       │ │ │   ·预览/编辑   │
│  ├ 角色       │  │ └──────────────────────┘ │ │   ·[应用][丢弃] │
│  │ └记忆      │  │ 字数: 2,341 / 3,000  ▓▓▓░│ │  ③ 记忆注入预览 │
│  ├ 大纲       │  └──────────────────────────┘ │   ·POV 经历命中 │
│  ├ 章节       │                                │   ·知识状态命中 │
│  ├ 记忆库      │                                │   ·承诺提醒命中 │
│  ├ 教程库      │                                │  ④ 设定沉淀卡   │
│  ├ 提示词      │                                │   ·候选新设定列表│
│  └ 修订检查    │                                │   ·勾选入库      │
├──────────────┴────────────────────────────────┴────────────────┤
│ 状态栏  档位: Std(deepseek-chat)｜队列: 0｜本次 token: 1.2k｜已保存     │
└────────────────────────────────────────────────────────────────┘
```

### 7.2 各模块界面

| 模块 | 主要视图 | 关键交互 |
|---|---|---|
| 仪表盘 | 项目进度卡片（点子/世界观/角色/大纲/章节/字数统计/记忆覆盖率） | 一键"从点子继续"，跳转到下一未完成步骤 |
| 点子 | 卡片流视图，筛选器（按状态/标签） | 每卡上"AI 扩展 / AI 评估 / 转项目"按钮 |
| 世界观 | 左侧分类树 + 右侧条目编辑器（双栏） | 条目详情页含"AI 扩写 / 校验铁律 / 标记铁律" |
| 角色 | 角色列表 + 角色卡表单 + 关系图（力导向图） | "AI 生成角色"向导；关系图节点可拖拽 |
| **角色·记忆** | 角色详情页标签页：**经历时间线 / 知识清单 / 承诺清单** | 时间线条目点击跳转对应章节正文；知识清单可切换"知道/不知道"视图并手动修正；承诺可标记回收 |
| 大纲 | 卷树（QTreeView）+ 卷详情编辑 | "AI 生成大纲"向导：选择主题与角色后流式生成 |
| 章节 | 章列表（表格：序/标题/POV/目标/状态/字数/记忆状态） | 批量"AI 拆章"；右键"生成细纲"；POV 可在表格内直接修改 |
| 细纲 | 节拍列表（可拖拽排序）+ 节拍编辑表单 | 每节拍"AI 扩写为场景"；"重新生成"；节拍可关联信息项 |
| 正文 | Markdown 编辑器 + 右侧建议草稿面板 | 光标处"续写"；选中文本"润色/扩写"；进度条；助手面板③展示本次注入的记忆命中项；生成前可展开参数面板（字数/风格/视角单次覆盖） |
| **记忆库** | 全知"剧情事件日志"视图（按章浏览事件）+ 记忆修正面板 + 记忆提取任务列表 | 事件条目点击跳转正文与角色经历；修正后触发重提取 |
| **设定沉淀** | 候选新设定列表（分类/标题/摘要/重要度/出处） | 接受生成内容后自动弹出；勾选入库、编辑摘要、标记铁律；可整批接受/拒绝 |
| **AI 指令** | 全局指令栏 + 指令执行面板 | `Ctrl+J` 唤起；修改计划逐条勾选确认；变更摘要展示；一键回退 |
| **教程库** | 教程文档列表 + 分类筛选 + 导入（粘贴/文件/URL）+ 预览 | 导入后自动切片；可启用/停用；绑定项目或全局 |
| **提示词管理** | 三级模板列表 + 内嵌编辑器 + 版本历史 | 语法高亮；保存生成版本快照；回滚/恢复默认 |
| **模型路由（设置页）** | 路由表编辑（任务 × 档位）、各 Provider 三档模型配置、覆盖优先级、自动降级开关 | 表格内直接改档位；档位单次覆盖在生成参数面板与工具栏均可操作 |
| 修订 | 问题清单（位置/问题/类型/建议）+ 逐条应用 | "开始全局扫描"后流式出结果；问题按六类过滤 |

### 7.3 交互规范

- **快捷键**：`Ctrl+Enter` 触发当前上下文 AI 生成；`Ctrl+Shift+Enter` 应用建议草稿；`Esc` 中断生成；`Ctrl+M` 触发记忆提取；**`Ctrl+J` 唤起全局 AI 指令栏**。
- **拖拽**：大纲树章节可在卷间拖拽；细纲节拍可拖拽排序。
- **双向跳转**：正文 ↔ 事件日志 ↔ 角色经历时间线 ↔ 承诺 ↔ 设定沉淀出处，任何一处点击均可跳到对应章节正文（通过 `chapter_id` / `event_ref` 关联）。
- **自动保存**：正文编辑器 30 秒防抖自动保存 + 关闭时强制保存；建议草稿保留在内存，崩溃后从 `generation_logs` 可查任务，但草稿不自动恢复（提示用户）。
- **深色/浅色主题**：跟随系统。

---

## 8. 关键流程时序

### 8.1 正文生成时序（含记忆注入）

```
用户点击"AI 续写"（光标在第 N 章第 k 节拍处，POV 角色 = 本章 pov_char_id）
  │
  ├─▶ 记忆注入器：
  │     ├─ POV 角色经历档案 top-8（时间×importance×关键词加权）
  │     ├─ 其他出场角色相关经历 top-3
  │     ├─ 知识状态：细纲标注的信息项 + 出场角色知晓关系
  │     ├─ 相关历史事件 top-3（FTS5：地点/人物/物品/关键词）
  │     └─ 未回收承诺/伏笔提醒（与本章相关）
  │
  ├─▶ 组装器：卷摘要 + 近3章摘要 + 最近1~2章正文 + FTS5世界观条目 top-3
  │           + 出场角色卡 + 文风指南 + 本章细纲剩余节拍 + 上述记忆
  │
  ├─▶ 预估 token，>预算则裁剪（POV 记忆与知识状态不可丢，可压缩）
  │
  ├─▶ LLM 流式请求（异步，不阻塞 UI；Prompt 含三条记忆铁律）
  │       ├─ 逐段回调 → 建议草稿面板上屏；助手面板③同步展示记忆命中项
  │       ├─ 用户点 [中断] → 取消流，保留已生成草稿
  │       └─ 完成 → 任务卡转"完成"，可 [应用] 或 [重新生成]
  │
  ├─▶ 用户编辑/接受 → 写入 chapters.content，更新 word_count
  │
  └─▶ 触发后台任务：章节摘要更新 + 记忆提取（见 8.3）
```

### 8.2 大纲生成时序

```
用户点击"AI 生成大纲"
  ├─▶ 组装器：主题 + 文风 + 检索 top 世界观条目 + 主要角色卡
  ├─▶ LLM 结构化输出：卷列表 JSON（每卷含目标/冲突/收束）
  ├─▶ 校验 JSON schema → 失败自动重试 1 次
  ├─▶ 渲染为大纲树预览 → 用户逐卷确认或整体接受
  └─▶ 接受后写入 arcs 表，状态置 planned
```

### 8.3 记忆提取时序（每章定稿后 / 手动触发）

由 LangGraph 工作流图 `memory_extract_workflow` 编排（见 6.7.3），节点间状态自动传递、失败可断点续跑：

```
触发：章节 content_status 变为 first_draft/polished，或用户点击"记忆提取"
  │
  ├─▶ Workflow 节点 load_chapter：读取该章正文 + 细纲 + POV/出场角色
  │
  ├─▶ 节点 extract_events：抽取 story_events（LLM 结构化 JSON，含参与角色/新信息/伏笔）
  ├─▶ 节点 per_char_mem（循环）：按参与角色拆分生成 character_events（限知重述 + 台词原文）
  │        （同一事件不同角色的条目独立生成，体现"谁看到了什么"）
  ├─▶ 节点 validate：JSON schema 校验，失败自动重试 1 次
  ├─▶ 节点 update_know：更新 info_items / character_knowledge（新信息揭晓、知晓关系）
  ├─▶ 节点 update_prom：更新 promises（登记新 flag、推进旧 flag 状态）
  ├─▶ 节点 gen_summary：更新章节摘要（滚动摘要，仅概览用途）
  │
  ├─▶ 节点 interrupt：★ 用户审阅"记忆修正"面板（增删改任意条目）
  │        └─ 确认后 resume，进入 commit 节点
  │
  ├─▶ 节点 commit：全部写入后 chapters.memory_status = 'extracted'
  │
  └─▶ 用户修改记忆后置 memory_status = 'needs_refresh'，支持一键重提取
      （checkpoint 可复用：重提取只重跑受影响节点）
```

### 8.4 AI 指令执行时序（v1.3，edit_command_workflow）

```
用户 Ctrl+J 打开指令栏，输入："把反派的身份改成主角失散多年的哥哥，并同步调整前 3 章伏笔"
  │
  ├─▶ parse_intent：识别对象（反派=角色卡X、主角=角色卡Y）、动作（改身份）、范围（前3章）
  │
  ├─▶ tool_search（只读，多次）：
  │     读取角色卡X/Y、两人的经历档案、知识状态（"身世"信息项归属）、
  │     前 3 章正文中与"身份/伏笔"相关的片段
  │
  ├─▶ build_plan：LLM 产出【修改计划】
  │     ·影响范围：角色卡X、信息项、第1/2/3章正文与细纲、角色关系
  │     ·逐条变更项：如「角色卡X.profile.背景 → 补"真实身份为Y的哥哥"」、
  │      「第2章对话段 → 增加暗示台词」...
  │     ·新增设定建议：「主角身世真相」→ 设定沉淀候选
  │
  ├─▶ interrupt：指令面板展示计划，用户勾选/编辑/取消个别变更项后确认
  │
  ├─▶ tool_apply：仅执行确认的变更项（每数据域先备份）
  │
  └─▶ summarize+sync：变更摘要展示；第1~3章标记记忆需重提取；
      新设定进入沉淀候选；记 generation_logs（module=command）
```

### 8.5 设定沉淀时序（v1.3，canon_capture_workflow）

```
用户点击"应用"接受正文/大纲/细纲生成内容
  │
  ├─▶ 触发 canon_capture_workflow
  ├─▶ 节点 detect：LLM 对照现有 world_entries 提取新专有设定
  │     （正文生成 prompt 已要求以 {{设定名}} 标记新设定，此处优先取标记）
  ├─▶ 节点 dedupe：与设定库 FTS5 比对去重（同名/近似名 → 合并或提示）
  ├─▶ 节点 build_candidates：产出候选列表（分类/标题/摘要/重要度/出处片段）
  ├─▶ 节点 interrupt：★ 用户勾选入库项、编辑摘要、标记铁律、调整分类
  ├─▶ 节点 commit：写入 world_entries；重要度高且涉秘密的顺带建议 info_items
  └─▶ 完成后：新条目立即可被后续生成检索到
```

---

## 9. 非功能需求

| 类别 | 需求 |
|---|---|
| 性能 | UI 操作 < 100ms 响应；LLM 请求全程异步不冻结界面；10 万汉字级项目启动 < 2s；记忆检索单次 < 50ms（FTS5） |
| 可靠性 | SQLite WAL 模式 + 每日自动备份（`data/backups/` 保留 30 份）；生成任务失败可重试/续生成；记忆提取失败不影响正文数据（独立事务） |
| 可扩展性 | LLM 层协议化可换厂商；Prompt 模板热更新；检索后端可替换（FTS5 → 向量）；记忆检索可升级为 embedding 语义检索 |
| 安全与隐私 | API Key 存系统钥匙串/本地加密配置，不写入数据库与日志；数据不出本机（仅 API 调用发送必要内容） |
| 可维护性 | 分层架构、日志分级（记录每次 LLM 调用摘要与错误）、`generation_logs` 全留痕 |
| 兼容性 | Windows 10/11 为主，兼顾 macOS/Linux（PySide6 跨平台）；模型配置兼容主流 OpenAI 兼容厂商 |

---

## 10. 项目规划

### 10.1 里程碑

| 阶段 | 周期 | 交付内容 | 验收标准 |
|---|---|---|---|
| M0 技术验证 | 1 周 | PySide6 骨架 + LLM 流式调用 + SQLite 建库 | 界面启动、输入提示词流式上屏、可中断 |
| M1 MVP | 4 周 | 项目/点子/世界观/角色/大纲/章节 CRUD + 基础 AI 生成（简单上下文拼接）+ 正文编辑器 + **生成配置**（作品级默认 + 单次覆盖） | 走通"点子 → 大纲 → 细纲 → 3 章正文"完整链路；生成受目标字数/风格约束 |
| M2 上下文与体验 | 3 周 | 上下文组装器 v1（含【生成配置】【写作技巧参考】注入）、FTS5 检索注入、滚动摘要、细纲节拍、建议草稿面板、批量拆章/生成、**教程知识库导入与检索** | 第 20 章生成质量与第 1 章持平（人工评测）；导入教程后生成风格明显受教程影响 |
| **M3 记忆体系** | 3 周 | **三层记忆建表与提取流水线（LangGraph `memory_extract_workflow`）、记忆注入器、角色记忆视图、事件日志视图、知识/承诺管理、workflows 封装层** | 写满 30 章后：① POV 角色言行与 1~30 章经历档案零冲突（抽查 10 处）；② 知识泄漏扫描 0 命中；③ 提取中途退出后可 checkpoint 续跑 |
| M4 修订与打磨 | 3 周 | 六类一致性扫描、**AI 指令驱动编辑（edit_command_workflow + 工具注册表）**、**设定沉淀（canon_capture_workflow）**、**提示词管理界面（三级模板+版本历史）**、Token 用量面板、备份恢复、打包 exe | 指令"改身份并同步伏笔"类操作可在 3 分钟内出计划并完成执行；全局扫描 10 分钟内出报告；打包产物可独立运行 |

### 10.2 建议目录结构

```
novel_studio/
├── main.py                  # 入口：初始化配置/DB/主窗口
├── config.toml              # 模型配置（base_url/api_key/model/温度）
├── app/
│   ├── core/                # config.py, logger.py, events.py, constants.py
│   ├── db/                  # ★ 数据库唯一访问点（见 4.4）
│   │   ├── session.py       # Session 工厂/上下文（唯一会话入口）
│   │   ├── models.py        # SQLAlchemy 模型
│   │   ├── migrations.py    # 迁移脚本（唯一允许裸 DDL）
│   │   ├── fts.py           # FTS5 索引维护
│   │   ├── unit_of_work.py  # 事务边界（提交/回滚）
│   │   └── repositories/    # 仓储层（唯一数据库访问点）
│   │       ├── base.py      # 通用 CRUD 基类
│   │       ├── idea_repo.py, world_repo.py, character_repo.py
│   │       ├── chapter_repo.py, memory_repo.py, canon_repo.py
│   │       └── handbook_repo.py, prompt_repo.py, generation_log_repo.py
│   ├── llm/                 # client.py, prompts.py, assembler.py,
│   │                        # memory_injector.py, json_utils.py, usage.py
│   ├── services/            # idea_svc.py, world_svc.py, character_svc.py,
│   │                        # outline_svc.py, chapter_svc.py, draft_svc.py,
│   │                        # check_svc.py, memory_svc.py,
│   │                        # canon_capture_svc.py, handbook_svc.py,
│   │                        # command_svc.py
│   ├── workflows/           # registry.py（run_workflow 统一入口）,
│   │                        # memory_extract_graph.py, canon_capture_graph.py,
│   │                        # edit_command_graph.py, consistency_scan_graph.py,
│   │                        # batch_draft_graph.py
│   ├── tools/               # 指令 Agent 工具注册表（只读检索 / 写入 apply）
│   ├── workers/             # task_queue.py, streaming.py, canceller.py,
│   │                        # 记忆提取等任务的 worker 入口（薄层，逻辑在图内）
│   ├── ui/                  # main_window.py, views/（含 memory_views.py,
│   │                        # event_log_view.py）, assistant_panel.py, settings_dlg.py
│   └── utils/               # token_estimate.py, markdown_helpers.py
├── data/                    # novel.db, backups/（运行时生成）
├── resources/               # 图标、内置 Prompt 模板 JSON
└── tests/
```

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 上下文窗口限制导致长篇一致性崩坏 | 核心价值受损 | **三层记忆体系**（经历档案/事件日志/知识状态）+ 检索注入 + 按预算裁剪 + 六类一致性扫描兜底 |
| **记忆提取质量不稳（漏提/错提/台词失真）** | 记忆库污染，反而引入错误约束 | 台词原文引用（quotes 字段）；提取走低温度+JSON schema 校验；记忆修正面板 + needs_refresh 重提取；扫描时对"记忆冲突"给出依据原文，供用户裁决 |
| **记忆库膨胀**（50 万字 → 每角色数百条经历） | 检索噪声、注入超预算 | importance 分级 + 时间衰减 + FTS5 命中加权，只注入 top-k；记忆库独立于正文存储，不随正文进入上下文 |
| API 成本随写作量线性增长 | 用户弃用 | token 预估与用量面板、草稿/精修双模式、记忆提取走低成本模型、生成日志审计 |
| 生成内容幻觉（设定漂移/事实错误） | 需人工返工 | 结构化输出 + schema 校验、注入铁律设定与经历原文、六类一致性扫描 |
| 长任务中断（断网/超时） | 生成失败 | 指数退避重试、断点续生成（工作流图 checkpoint 续跑）、草稿保留、任务日志 |
| **LangGraph 依赖与学习成本** | 引入重依赖、版本漂移、调试成本 | **仅多步流程使用**（单次调用保持直连）；`app/workflows/` 封装层隔离 LangGraph API；锁定精确版本并单独验证升级 |
| **AI 指令误改风险**（实体指代错误/计划错误导致误改数据） | 用户数据被破坏 | 修改计划逐条确认制（默认全量预览）；每类数据域执行前备份、可回退；实体消歧失败先向用户提问；强模型低温度执行 |
| **设定沉淀噪声**（误把非设定内容当新设定 / 与既有设定重复） | 设定库污染 | 与设定库 FTS5 去重比对；候选列表用户勾选制；设置中可开"自动沉淀低风险项"；沉淀条目可在世界观页正常编辑删除 |
| **教程注入稀释上下文 / 内容冲突** | 生成质量下降、token 浪费 | 教程区块预算固定 ~1k 且**裁剪时最先丢弃**；按任务类型过滤分类；教程内容与项目 base_prompt 冲突时以项目设置为准（教程仅作参考，不视为铁律） |
| 单一模型能力瓶颈 | 规划/写作质量不均 | **模型路由**（6.1.1）：按任务难度/重要度路由 lite/standard/pro，可跨厂商；高价值任务（大纲/精修正文/扫描/指令）走强模型，结构化任务走低成本模型；`auto_downgrade` 兜底限流 |
| **模型路由误配**（档位选错/降级导致质量波动） | 质量与成本失衡 | 内置路由表按任务类型给出合理默认；覆盖优先级明确（单次 > 项目 > 全局 > 默认）；降级自动提示用户；`generation_logs` 记录实际 tier 供审计 |
| 用户上手门槛 | 功能多而杂 | 仪表盘引导式工作流、分步向导、内置模板、记忆体系默认"自动提取"无需用户干预 |

---

## 12. 附录

### 12.1 正文生成 Prompt 模板示例（核心，含记忆注入）

```
你是长篇小说写手，文风需严格遵循作品风格指南。
【风格指南】{{style_guide}}
【生成配置】{{generation_profile}}          ← v1.3：目标字数/视角/节奏/对话密度等
【写作技巧参考】{{handbook_hits}}            ← v1.3：教程知识库检索命中（可能为空）
【当前卷大纲】{{arc_outline}}
【本章目标】{{chapter_objective}}
【本章视点角色】{{pov_character}}
【本章细纲（剩余节拍）】{{remaining_beats}}
【世界观设定（检索命中）】
{{retrieved_entries}}
【出场角色】
{{cast_cards}}
【视点角色经历档案（最近与相关）】
{{pov_memories}}
【其他角色相关经历】
{{other_char_events}}
【知识状态（谁知道什么——写作必须严格遵守）】
{{knowledge_state}}
【相关历史事件】
{{related_events}}
【未回收承诺/伏笔提醒】
{{open_promises}}
【前情提要（概览）】
{{rolling_summary}}
【最近正文】
{{recent_draft}}
【任务】续写 {{target_beat}} 对应的内容：

三条记忆铁律（违反任一条即不合格）：
① 角色言行必须与【视点角色经历档案】连续：不得遗忘、否认或改写前文已发生之事、已说之话；
② 角色只能表达【知识状态】中其知晓范围内的信息：不得说出/表现出不该知道的事；
③ 若需复述角色说过的话，必须沿用经历档案【quotes】中的原文，不得自行改写。

设定标记约定（v1.3）：文中首次出现的、设定库之外的新专有设定（地点/物品/规则/组织/人物等），
用 {{设定名}} 括起来标记，便于后续设定沉淀提取；已存在于设定库的设定直接使用、不得加标记。

其余要求：落实节拍目标，铺垫后续伏笔；保持角色说话风格与视角一致；
输出流畅的叙事文字，不分节标题。
```

### 12.2 记忆提取 Prompt 模板示例（事件→经历→知识→承诺）

```
你是小说记忆整理员。请阅读给定章节正文，产出四类结构化结果：
① 剧情事件（全知视角，2~6 条）：标题、一句话摘要、地点、参与角色、本事件暴露的新信息、埋下/推进的伏笔
② 角色经历（按每个参与角色独立生成，限知视角）：
   summary（该角色视角下发生了什么）、quotes（该角色说过的关键台词【必须逐字引用原文】）、
   acted（做过的事/决定）、felt（情绪变化）、got_lost（获得/失去的物品能力伤势关系）、
   secrets（知道但没说出口的）
③ 信息项更新：新出现的秘密/真相/计划（title、content），以及"谁从本章起知道了它"
④ 承诺/伏笔：新立的 flag 与既有 flag 的状态变化（open→pending→fulfilled）

【出场角色与角色卡】
{{cast_cards}}
【章节正文】
{{chapter_content}}
【输出格式】严格 JSON：
{"events": [...], "character_events": [...], "info_updates": [...], "promise_updates": [...]}
```

### 12.3 一致性扫描 Prompt 模板示例（六类）

```
你是编辑审校。请对照"作品铁律设定""角色卡""角色经历档案""知识状态""承诺跟踪"，
审查给定正文，找出六类问题：
① timeline 时间线矛盾  ② setting 设定冲突  ③ character 角色行为/性格不符
④ memory_conflict 角色否认/遗忘前文已发生之事、已说之话（对照经历档案原文）
⑤ knowledge_leak 角色说出/表现出本不该知道的信息（对照知识状态）
⑥ promise_break 承诺/伏笔该回收未回收

【铁律设定】{{canonical_entries}}
【角色卡】{{cast_cards}}
【角色经历档案（出场角色）】{{character_memories}}
【知识状态】{{knowledge_state}}
【未回收承诺】{{open_promises}}
【待审正文】{{draft_text}}
【输出格式】严格 JSON 数组：
[{"chapter": N, "quote": "原文片段", "type": "timeline|setting|character|memory_conflict|knowledge_leak|promise_break",
  "issue": "问题描述（含与哪条经历/知识的冲突依据）", "suggestion": "修改建议"}]
```

### 12.4 设定提取 Prompt 模板示例（设定沉淀，v1.3）

```
你是世界观设定管理员。请从给定生成内容中，提取"设定库中尚不存在的新专有设定"。
判定标准：专有名词（地点/人物/组织/物品/功法/种族/规则/事件），且不是既有设定的别名或重复。
优先提取正文中以 {{设定名}} 标记的内容；未标记的由你自行判断。

【既有设定库（用于去重）】
{{existing_entries}}

【生成内容】
{{generated_text}}

【输出格式】严格 JSON 数组：
[{"title": "...", "category": "地理|规则|组织|物品|人物|事件|其他",
  "content": "不超过 80 字的内容摘要", "importance": 1-5,
  "source_snippet": "出处原文片段"}]
```

### 12.5 指令执行 Agent 系统提示词示例（edit_command_workflow，v1.3）

```
你是小说项目的"编辑助手"Agent。用户用自然语言下达修改指令，你的职责：
① 精确定位指令涉及的对象（角色/章节/设定条目/信息项），指代不明时输出待澄清问题，不得猜测；
② 只读检索相关数据（设定库、角色卡、经历档案、知识状态、正文），评估修改的影响范围；
③ 产出【修改计划】JSON：影响范围摘要 + 逐条变更项（数据域/对象/原值/新值/同步影响）+ 新增设定建议；
④ 绝不直接写入任何数据——写入必须等待用户对计划的确认；
⑤ 变更项必须考虑连锁影响：角色改动 → 相关经历/知识/承诺；设定改动 → 引用它的章节；
⑥ 新增设定进入"设定沉淀"候选，不在计划里直接创建条目。

【可用的只读工具】{{read_tools}}
【可用的写入工具】{{apply_tools}}
【用户指令】{{user_command}}
【检索到的相关数据】{{retrieved_data}}
【输出格式】严格 JSON：{"clarification"?: "...", "impact_summary": "...",
  "changes": [{"domain": "...", "target": "...", "before": "...", "after": "...", "note": "..."}],
  "new_settings": [...]}
```

### 12.6 参考

- OpenAI API 兼容协议（`/chat/completions`、`stream`、`response_format`）
- DeepSeek / 阿里云百炼（通义）/ 智谱 GLM / Moonshot Kimi 均提供 OpenAI 兼容端点
- SQLite FTS5 全文检索文档
- Qt for Python (PySide6) 官方文档
